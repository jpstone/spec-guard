{
  "name": "@jpstone/spec-guard",
  "version": "3.1.0",
  "private": false,
  "description": "Local governance layer for agentic implementation workflows: a role-based implementation loop with human gates, a deterministic state machine, and a focused review loop.",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "bin": {
    "spec-guard": "packages/cli/dist/index.js",
    "spec-guard-mcp": "packages/mcp/dist/index.js"
  },
  "files": [
    "packages/cli/dist",
    "packages/mcp/dist",
    "packages/core/dist",
    "packages/viewer/dist",
    "packages/*/package.json",
    "README.md"
  ],
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -p packages/core/tsconfig.build.json && tsc -p packages/viewer/tsconfig.build.json && tsc -p packages/mcp/tsconfig.build.json && tsc -p packages/cli/tsconfig.build.json",
    "test": "npm run build && vitest run",
    "typecheck": "tsc -p tsconfig.base.json --noEmit",
    "lint": "tsc -p tsconfig.base.json --noEmit",
    "prepublishOnly": "npm run build && npm run typecheck && vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  },
  "dependencies": {
    "@spec-guard/core": "file:packages/core",
    "@spec-guard/viewer": "file:packages/viewer",
    "@spec-guard/mcp": "file:packages/mcp",
    "zod": "^4.1.13"
  }
}
