# Tests Outline

Write before implementation:

```text
render <EmptyState title="No projects" /> exposes heading "No projects"
render with description shows the description text
render with actionLabel and onAction exposes button by role/name
clicking action button calls onAction
```

## Expected Initial Failure

The component does not exist or does not satisfy the documented component API.
